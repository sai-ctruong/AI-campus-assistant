# Ảnh demo cho README

Thả các ảnh chụp màn hình app vào thư mục này, đặt đúng tên để README hiển thị:

| Tên file | Màn hình |
|---|---|
| `library.png`   | Trang Library (upload + danh sách tài liệu) |
| `chat.png`      | Chat + panel trích dẫn nguồn |
| `quiz.png`      | Làm quiz |
| `dashboard.png` | Dashboard tiến độ |
| `notebook.png`  | Giải thích notebook |

Gợi ý: chụp ở chế độ full màn, tỉ lệ ~16:10, PNG. Sau khi thêm, ảnh sẽ tự hiện trong `README.md`.
